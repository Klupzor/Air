# Modules

| Module | Owns |
|---|---|
| `src/models.py` | The `Task` entity and `Status` definitions |
| `src/store.py` | In-memory storage, and the status-filtering rule (`blueprint/rules.md#r2`) |
| `src/cli.py` | User-facing command-line I/O only — no business logic |
