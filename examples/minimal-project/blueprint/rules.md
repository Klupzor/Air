# Rules

## R1: New tasks default to `todo`

A task created without an explicit status starts as `todo`.

## R2: Status filtering is an exact match

Listing tasks with a status filter returns only tasks whose status exactly equals the requested one — no partial or hierarchical matching.
